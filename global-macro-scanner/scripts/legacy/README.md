# scripts/legacy/

This directory holds superseded or experimental scripts that are no longer part of the active production pipeline. They are kept for historical reference only.

**Do not import from these files in production code.** If you need functionality from a script here, port it into the canonical path (`main.py`, `db.py`, `scripts/etl/`, etc.) with maintainer approval.
