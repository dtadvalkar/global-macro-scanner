# Global Macro Scanner — Cleanup Progress Log

---

## Batch A1 — 2026-04-18

**Scope:** Phase A junk removal (verified generated artifacts only)

**Deleted:**
- `tests/api-exported-logs.txt`, `api-exported-logs_1.txt`, `api-exported-logs_2.txt`, `api-exported-logs_3.txt` — exported log dumps, no code references
- `tree.txt` — generated directory tree dump
- `final_run_output.txt` — generated run output dump
- `flatten_output.txt` — generated flatten output dump
- `requirements_backup.tx` — typo backup artifact (truncated `.tx` extension)
- 30× `*.ib_insync_backup` files across `backups/ib_insync/`, `data/`, `screener/ibkr/`, `tests/` — strategy: delete all (generated by ib_insync library during migration; not source code)

**Skipped:**
- `recent_catches.csv` — active writer found in `storage/csvlogging.py:13`; also referenced in `README.md`, `docs/user_guide/usage_examples.md`, `implementation_plan.md`. Do not delete.
- `data_files/processed/csv/recent_catches.csv` — same reason (writer in csvlogging.py targets root path; this copy may be a stale duplicate but skipped to be conservative)

**Ambiguities / follow-up for maintainer:**
- `data_files/processed/csv/recent_catches.csv` — unclear if this copy is ever written to (csvlogging.py writes to root `recent_catches.csv`). Maintainer may want to delete this copy in a later batch after confirming no writer targets it.
- `backups/ib_insync/` directory is now empty after `.ib_insync_backup` deletions; maintainer may want to remove the directory itself in a later batch.

**Next batch:** B1 (create `scripts/legacy/`, `scripts/ops/`, `docs/archive/` directories)

---

## Batch B1 — 2026-04-18

**Scope:** Directory scaffolding only (Phase C prerequisite)

**Created:**
- `scripts/legacy/` — for superseded scripts no longer in the active pipeline
- `scripts/legacy/README.md` — guardrail note: do not import from production; port if needed
- `scripts/ops/` — for operational helpers still useful but not part of daily orchestration
- `docs/archive/` — for completed one-time checklists and retired planning docs

**Already existed:**
- `docs/tasks/` — created during Batch A1

**No files moved or deleted in this batch.**

**Next batch:** B2 (move one category of legacy files — IBKR flatteners or old mains)

---

## Batch B2 — 2026-04-18 (SKIPPED)

**Scope:** Move one category of legacy files — IBKR flatteners (Option 1) or old mains (Option 2)

**Option 1 — IBKR flatteners:** `flatten_ibkr_final.py` and `flatten_ibkr_market_data.py` are NOT duplicates. They target different tables (`stock_fundamentals` vs `current_market_data`) and serve distinct purposes. `flatten_ibkr_final.py` is still the only script populating `stock_fundamentals` from IBKR XML; moving it to legacy would be incorrect. No zero-reference candidates found.

**Option 2 — Legacy mains:** All three `main/` files have active references. `main-paper.py` is the paper-trading entrypoint (distinct purpose). `main_automated.py` is hardcoded in `scheduler/market_scheduler.py:243`. `main_intelligent.py` is documented as the manual intelligent-scheduling path. Both automated and intelligent mains are nearly identical in logic but serve subtly different invocation contexts (scheduler vs manual). Maintainer confirmed all three should be retained.

**Decision:** Skip B2 — no safe candidates for this batch. No files moved.

**Next batch:** C1 (delete orphaned files under scripts/legacy/)

---

## Batch B2 (retry) — 2026-04-18

**Scope:** Retire main_intelligent.py — maintainer decision after code review

**Evidence for retirement:**
- Functionally near-identical to `main/main_automated.py` (same scheduler call, same pipeline)
- Latent `NameError` bug: `os.getenv()` called inside `run_intelligent_scheduler()` but `os` only imported inside `if __name__ == '__main__'`
- Uses fragile env-var dispatch (`INTELLIGENT_SCHEDULING=true`) instead of proper CLI flags
- No error handling in `scan_markets()` (bare calls, no try/except)
- `scheduler/market_scheduler.py:243` hardcodes `main_automated.py` as the scheduled runner

**Moved:**
- `main/main_intelligent.py` → `scripts/legacy/main_intelligent.py`

**References updated (same batch):**
- `README.md:99` — replaced tree entry with `main_automated.py`
- `docs/market_scheduling_guide.md:40` — `INTELLIGENT_SCHEDULING=true python main_intelligent.py` → `python main/main_automated.py`
- `docs/market_scheduling_guide.md:57` — schtasks example updated to `main/main_automated.py`
- `docs/market_scheduling_guide.md:66` — cron example updated to `main/main_automated.py`
- `docs/market_scheduling_guide.md:207` — test command → `python main/main_automated.py --test-scheduler`
- `docs/market_scheduling_guide.md:230` — files list updated; legacy note added
- `docs/market_scheduling_guide.md:242` — quick-start command updated to `main/main_automated.py`

**Next batch:** C1 (scripts/legacy/ now has one file; can delete if zero external references)

---

## Batch C1 — 2026-04-18

**Scope:** Delete orphaned files from scripts/legacy/ (at most 5 per run)

**Reference check:** `main_intelligent.py` had zero functional references — only the progress log (excluded per C1 rules) and a parenthetical note in `docs/market_scheduling_guide.md:230` (cleaned up in same batch).

**Deleted:**
- `scripts/legacy/main_intelligent.py` — zero functional references; superseded by `main/main_automated.py`; latent `NameError` bug; no scheduler/import/subprocess usage

**Also cleaned up:**
- `docs/market_scheduling_guide.md:230` — removed stale `scripts/legacy/` cross-reference note

**Run complete.** All batches A1–C1 executed. Cleanup run closed.

---

## Batch D1 — 2026-04-18 (no-op)

All reference updates were done in the same batch as the moves (B2/C1). No broken paths remained to fix.

---

## Batch E — 2026-04-18 (Final documentation pass)

**Scope:** Verify and correct canonical paths across all key docs; add script taxonomy.

**README.md fixes:**
- ASCII diagram: `flatten_ibkr_mega.py` (phantom) → `scripts/etl/ibkr/flatten_ibkr_final.py`
- ETL scripts table: same phantom corrected
- Project structure tree: added `scripts/legacy/`, `scripts/ops/`, `docs/archive/`, `docs/tasks/`; noted `backups/ib_insync/` is now empty
- `utils/` tree indentation corrected (was misaligned)

**docs/master_development_plan.md:**
- Added **Script Taxonomy** table under Architecture Components covering all canonical, ops, and legacy/archive paths

**docs/developer_guide/etl_pipeline.md:**
- ETL scripts table: `flatten_ibkr_mega.py` (phantom) → `scripts/etl/ibkr/flatten_ibkr_final.py`

**docs/developer_guide/architecture.md:** Already accurate — no changes needed.
**tests/README.md:** No test helper paths moved — no changes needed.

**Cleanup run fully complete. All phases A–E executed.**

---

## Follow-up fixes — 2026-04-18

**scheduler/market_scheduler.py:243**
- Fixed broken path in `create_windows_task_scheduler_script()`: `main_automated.py` was referenced at repo root but was moved to `main/` during B2. Changed to `os.path.join(script_dir, 'main', 'main_automated.py')`.
- Assumption: `script_dir` resolves to repo root (confirmed — it is `dirname(dirname(abspath(__file__)))`), so adding `'main'` as an intermediate segment is correct.

**docs/master_development_plan.md**
- Removed stray tool-output markup appended to the final line (`</contents>`, `</xai:function_call: write>`, `<parameter name="file_path">...`) left over from a prior automated write.

---

## Trust Restoration Pass — 2026-04-18

**Source:** `.cursor/plans/REPO_TRUST_RESTORATION_PLAN.md`

### Changes made

**`main/main_automated.py`**
- Fixed broken `sys.path.insert`: was inserting `main/` directory (wrong); now inserts scanner root (`dirname(dirname(__file__))`). All imports from `config/`, `screener/`, etc. now resolve correctly when this file is run.
- Classification: **secondary scheduler runner** (called by `scheduler/market_scheduler.py`; not the primary daily runner).

**`data_files/processed/logs/` (8 files)**
- Untracked with `git rm --cached` (local copies preserved). Files are debug operational output, not source fixtures.
- Files: `bad_ticker_log.txt`, `bad_ticker_log_v2.txt`, `comparison_output.txt`, `debug_output.txt`, `final_comparison.txt`, `output.txt`, `ptt_contracts.txt`, `scan_log.txt`.

**`global-macro-scanner/.gitignore`**
- Added `data_files/processed/logs/` and `data_files/processed/csv/` to prevent future re-tracking of runtime outputs.

**`pyproject.toml` (outer root)**
- Removed scanner runtime dependencies (`pandas`, `python-telegram-bot`, `requests`, `schedule`, `yfinance`). These belong to `global-macro-scanner/requirements.txt`. Outer `pyproject.toml` now describes monorepo metadata only.
- Note: `uv.lock` at outer root is now stale and should be regenerated (`uv lock`) or deleted if the outer project has no real dependencies.

**`test_telegram.py` (outer root)**
- Moved to `global-macro-scanner/scripts/utils/test_telegram.py`. No code references existed; functionality is duplicated by `scripts/testing/diagnostics.py::test_telegram()`. Outer root copy removed from git.

**`README.md` (outer root)**
- Rewrote with a clear repo layout table, canonical env/venv/.env locations, and getting-started commands. Outer root is now unambiguously a monorepo shell.

**`global-macro-scanner/CLAUDE.md`**
- Made canonical venv (`global-macro-scanner/.venv`), dependency file (`requirements.txt`), and secrets file (`.env`) explicit. Previously said "repo root" without clarifying which root.

### Intentionally left unchanged

- `data_files/raw/xml/*.xml` and `data_files/raw/json/full_fd_data.json` — classified as **fixture/sample data** (small files, useful as reference inputs for ETL development). Remain tracked.
- `check_data_completeness.py` — tracked diagnostic at scanner root. Out of scope for this pass; classified as **ops/diagnostic**.
- `main/main-paper.py` — no changes; canonical paper-trading entrypoint.
- `scheduler/market_scheduler.py` — no changes beyond prior sys.path fix in `create_windows_task_scheduler_script()`.
- All `.env` values and DB credentials — untouched.
- `.venv/` — untouched.

### Verified script taxonomy

| Script | Classification |
|--------|---------------|
| `global-macro-scanner/main.py` | **Canonical runner** — primary daily orchestration entrypoint |
| `main/main_automated.py` | **Secondary scheduler runner** — invoked by `scheduler/market_scheduler.py`; sys.path bug now fixed |
| `main/main-paper.py` | **Canonical runner** — IBKR paper-trading mode |
| `scheduler/market_scheduler.py` | **Canonical scheduler** — intelligent market timing |
| `scripts/etl/ibkr/collect_daily_ibkr_market_data.py` | **Canonical ETL** — daily IBKR market data collection |
| `scripts/etl/ibkr/flatten_ibkr_market_data.py` | **Canonical ETL** — flattens IBKR snapshots → `current_market_data` |
| `scripts/etl/ibkr/flatten_ibkr_final.py` | **Canonical ETL** — flattens IBKR XML ratios → `stock_fundamentals` |
| `scripts/etl/yfinance/collect_historical_yfinance.py` | **Canonical ETL** — bulk YFinance OHLCV → `prices_daily` |
| `scripts/etl/yfinance/collect_daily_yfinance.py` | **ETL** — daily YFinance collection (verify if active) |
| `check_data_completeness.py` | **Ops/diagnostic** — tracked at scanner root; belongs in `scripts/testing/` |
| `scripts/utils/test_telegram.py` | **Ops/diagnostic** — Telegram connectivity test (moved from outer root) |

### Remaining risks / follow-up items

1. `uv.lock` at outer root is now stale (dependencies removed from `pyproject.toml`). Run `uv lock` or delete it if the outer project has no real runtime.
2. `main/main_automated.py` sys.path fix has not been smoke-tested end-to-end (requires TWS). Treat as secondary runner until confirmed.
3. `scripts/etl/yfinance/collect_daily_yfinance.py` — not yet verified as active vs. legacy.
4. `check_data_completeness.py` at scanner root is a loose diagnostic. Should move to `scripts/testing/` in a future pass.
5. Outer `scripts/` directory is empty — can be removed if no files are planned there, or kept as a placeholder.

---

## Follow-up fixes — 2026-04-18 (post trust-restoration)

**`README.md` (outer root)**
- Removed non-existent `cp .env.example .env` step; replaced with a comment listing the required env var names so a new engineer knows what to populate without implying an example file exists.

**`global-macro-scanner/scripts/utils/test_telegram.py`**
- Fixed env var from `TELEGRAM_CHAT_ID` → `CHAT_ID` to match the rest of the project (`config/settings.py:31`, `scripts/testing/diagnostics.py:27`). Updated the missing-var warning message to match.

---

## Doc unification pass — 2026-04-18

**Hierarchy enforced:**
1. Outer `README.md` — monorepo shell only
2. `global-macro-scanner/README.md` — canonical operator/developer doc
3. `global-macro-scanner/CLAUDE.md` — agent workflow rules only
4. `docs/user_guide/` topic docs — defer to README for setup

**`README.md` (outer root)**
- Removed getting-started code block; replaced with a single pointer to `global-macro-scanner/README.md`.

**`global-macro-scanner/README.md`**
- Fixed Python version: 3.10+ → 3.12
- Fixed venv name: `venv` → `.venv` (with Windows/macOS activate lines)
- Reclassified `main/main_automated.py` label: "canonical live runner" → "secondary scheduler runner"
- Removed "Generated by Antigravity" footer

**`global-macro-scanner/CLAUDE.md`**
- Trimmed §3 from a second README into agent-only rules: venv/deps/secrets rules + UTF-8 note + typical commands. Setup narrative removed; pointer to `README.md` added.

**`docs/user_guide/getting_started.md`**
- Added canonical-reference callout at top.

**`docs/user_guide/installation.md`**
- Added canonical-reference callout at top (notes this doc covers platform-specific detail).

**`docs/user_guide/troubleshooting.md`**
- Added canonical-reference callout at top.

**`docs/user_guide/usage_examples.md`** — no setup content; no changes needed.
